{
  "name": "critterbids-client",
  "private": true,
  "version": "0.0.0",
  "description": "CritterBids frontend monorepo (ADR 025). Workspace root for the shared wire-contract package, the bidder-facing SPA, the staff ops SPA, the seller console SPA, and the Playwright e2e harness.",
  "workspaces": [
    "shared",
    "bidder",
    "ops",
    "seller",
    "e2e"
  ],
  "scripts": {
    "e2e": "npm run test --workspace @critterbids/e2e"
  },
  "engines": {
    "node": ">=22"
  }
}
