{
  "name": "critterbids-client",
  "private": true,
  "version": "0.0.0",
  "description": "CritterBids frontend monorepo (ADR 025). Workspace root for the bidder-facing SPA, the staff ops SPA, and the Playwright e2e harness; the shared wire-contract package (shared/) stays a planned member — extraction deferred to the seller-console milestone (M8-S7 decision).",
  "workspaces": [
    "bidder",
    "ops",
    "e2e"
  ],
  "scripts": {
    "e2e": "npm run test --workspace @critterbids/e2e"
  },
  "engines": {
    "node": ">=22"
  }
}
