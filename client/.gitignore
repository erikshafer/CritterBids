# Frontend workspace ignores (ADR 025). The .NET solution at repo root has its own ignores.
node_modules/
dist/
.vite/
*.local
*.tsbuildinfo

# Playwright (client/e2e) run artifacts
test-results/
playwright-report/
blob-report/
